.DS_Store
*.backup
*.bak
.dotfiles_backup/

# Asla commit'lenmemesi gereken hassas dosyalar
id_rsa
id_ed25519
id_ecdsa
*.pem
*.key
.ssh/
.netrc
.aws/credentials
.npmrc
.env
.env.*
.zshrc.local
.zshenv.local

# zsh history & cache
.zsh_history
.zsh_sessions/
.zcompdump*

# Brewfile lock
Brewfile.lock.json
*.swp
*.swo

# Plugin manager auto-generated lock files (program kendisi yönetiyor)
lazy-lock.json
plugin/packer_compiled.lua

# Template artıkları
nvim/.config/nvim/README.md

# Tmux plugins are managed at runtime by TPM into ~/.tmux/plugins/
# (legacy path used by tmux.conf), not from the dotfiles repo.
tmux/.config/tmux/plugins/
